import Vuex from 'vuex'
import Vue from 'vue'

Vue.use(Vuex)

// const mutations = {...};
// const actions = {...};
const state = {
    Addr :  '123456'
};

export default new Vuex.Store({
    state
});
